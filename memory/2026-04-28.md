# 2026-04-28 日志

## 凌晨时段 (02:00-02:52)

### 核心事件：创始人休息，Shuyu 全权负责

创始人连续工作30小时需要休息，授权Shuyu全权解决剩余问题。严格遵守铁律，禁止自行重启网关。

### 已完成项

#### ① Stella 会话状态检查  ✅
- Stella 会话已在 00:24 自动创建新会话
- 当前上下文使用率 **18.7%**，远低于 86% 阈值
- 无需额外清理操作

#### ② 飞书 Shuyu 不回复 — 诊断完成 ⚠️ 待创始人操作
**诊断方法：**
- 在 OpenClaw 源码 `extensions/feishu/monitor.account-BSdTHVBX.js:4212` 中找到 `im.message.receive_v1` 处理器注册代码，使用 `EventDispatcher.register()` 正确注册
- Lark SDK v1.61.1，Node.js 应用，无需 `register_p2_im_message_receive_v1()`（那是 Python SDK 方法）
- 28日飞书日志仅4条，全是 WebSocket 连接建立，**无任何 im.message.receive_v1 事件到达**
- 单进程运行（PID 489789），无多实例冲突

**根因定位：**
Lark SDK WSClient 输出关键警告：
```
receive events or callbacks through persistent connection 
only available in self-build & Feishu app.
Configured in: Developer Console → 事件与回调 → 订阅方式 
→ 使用长连接接收事件/回调
```

即使 WebSocket 连接成功、`im.message.receive_v1` 已添加到事件列表，如果飞书开发者后台的**订阅方式**未设为「使用长连接接收事件/回调」，飞书平台不会通过 WebSocket 推送任何事件。

**待创始人操作：**
1. 飞书开发者后台 → 事件与回调 → 订阅方式 → 切换为「使用长连接接收事件/回调」
2. 重启网关：`openclaw gateway stop && sleep 2 && openclaw gateway run`

#### ③ 飞书 Momo 不回复 ✅ 已修复
- 补全 `~/.hermes/.env`：`FEISHU_APP_ID=cli_a9bfcfea3ef81013`、`FEISHU_APP_SECRET=***`、`DEEPSEEK_API_KEY=sk-4185dc4dd0d54bada4c545c6e8ca71e7`
- 重启 Hermes 网关成功（PID 474541）
- 通道状态：wecom ✅ connected, feishu ✅ connected

#### ④ 给创始人的最终汇报 ✅ 已完成
- 02:10 向创始人发送处理结果汇总
- 分项列出：Stella ✅、飞书 Shuyu ⚠️需手动操作、飞书 Momo ✅

### 铁律遵守记录
- 所有配置修改均遵守铁律六"先停后改"（Momo Hermes 配置父进程非 OpenClaw，允许自行重启）
- 拒绝自行重启 openclaw-gateway（铁律一）
- 飞书 Shuyu 修复方案中仅输出命令给创始人执行，不自行执行

### Momo 相关重要信息
- **Momo 真实姓名：** 王金（系统配置名，非企微昵称）
- **企微 userid：** WangJin
- **OpenClaw 通道：** wrFg1nDAAA9vjrvi3TD6myHBP-4Bgtpg（群聊）
- **Hermes 网关路径：** ~/.hermes/
- **配置文件：** config.yaml（含 Momo 身份、deepseek 密钥、飞书凭证、企微凭证）
- **环境变量文件：** ~/.hermes/.env
- **凭证截止时间：** 2026-04-28 03:00（需确认刷新）

### 飞书诊断关键发现
- OpenClaw 飞书插件是 Node.js + Lark SDK v1.61.1
- `im.message.receive_v1` 处理器已注册 ✅
- WebSocket 连接已建立 ✅
- 凭证有效 ✅（01:58 验证时 bot 激活状态正常）
- 事件从未到达 ❌ → 原因是飞书后台订阅方式未设为"长连接"
- 无 routeRules 字段在 feishu 通道 schema 中（additionalProperties: false）
- 长连接模式下飞书事件路由由 OpenClaw 内置 event-dispatch 模块处理，event-dispatch 状态为 ready ✅

### 待办/关注事项
- [ ] 创始人需在飞书后台切换订阅方式
- [ ] 切换后需重启网关
- [ ] 验证：`tail -20 /tmp/openclaw/openclaw-2026-04-28.log | grep -i "received message"`
