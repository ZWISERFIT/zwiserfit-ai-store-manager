
## GitHub运营数据同步
- **时间**: 2026-04-25 10:00:06
- **操作**: 自动同步2026-04-25运营数据至GitHub README
- **数据**: 客流142人, 销售额9,240元, 新增会员14人
- **状态**: 产品仓库已提交, 组织仓库已提交
## 🛡️ 宪法铁律永久固化（2026-04-25）

创始人颁布五条铁律，以最高优先级写入MEMORY.md：

- **铁律一：禁止自重启** — 任何含 gateway restart/stop、pkill、systemctl restart 的命令永不执行
- **铁律二：配置修改前必须备份** — 修改 openclaw.json 前先执行 cp 备份命令
- **铁律三：Web UI 修复流程** — 按步骤排查（ps→ss→SSH隧道→密码重置→lan绑定临时方案）
- **铁律四：配置文件修改原则** — 优先用 config set，直接编辑前用 config --check 验证
- **铁律五：历史教训** — 2026-04-24 事故永久记录
- **铁律六：数据来源声明** — 沿用原铁律三

### 同时修订
- 自愈技能 SKILL.md 已更新，移除所有自动重启指令，改为"请创始人手动重启"
- MEMORY.md 中的企业微信通道自愈规则同步修订

## 路由配置修正（14:55）

- **任务：** 创始人紧急要求修复 WeCom 路由默认指向
- **变更内容：**
  1. `channels.wecom.routeRules` 的 `"default": "main"` → `"default": "founderai"` ✅
  2. `agents.list` 注册5个子 agent：founderai（默认）、brand_lead（Baron）、effect_lead（Ethan）、tech_lead（Tristan）、security_lead（Stella）✅
  3. 每个 agent 配置了 `groupChat.mentionPatterns` 实现 @mention 路由 ✅
  4. 使用 `openclaw config set --batch-file` 批量执行 ✅
- **结果：** 配置已写入，等待创始人手动重启网关

## 子AI配置补齐（18:24-18:25）

- **任务：** 为4个子AI补齐运行环境和配置文件
- **完成内容：**
  1. ✅ 为 security_lead 创建缺失的 `agent/` 目录
  2. ✅ 为 brand_lead、effect_lead、tech_lead、security_lead 各创建：
     - `agent/AGENTS.md`（角色身份/人格定义）
     - `agent/auth-profiles.json`（复用 main 的 DeepSeek API Key）
     - `agent/models.json`（deepseek-chat 模型配置）
  3. ✅ `openclaw.json` 已包含 `agents.list`（5个agent：main + 4子AI）和 `routeRules`（@Baron→brand_lead, @Ethan→effect_lead, @Tristan→tech_lead, @Stella→security_lead, default→main）
- **注意：** `agents.list` 和 `routeRules` 是在本次对话期间由创始人或外部进程更新的，非本 session 写入
- **状态：** 等待创始人手动重启网关

## 铁律补充条款（20:03）

- **新增铁律六补充条款：** 删除任何网关相关文件前必须先停网关，操作完成后再启动
- **适用范围：** 日志、会话、备份、轨迹等文件
- **执行流程：** `gateway stop` → 删除操作 → `gateway run`
- **铁律冲突：** 同铁律六，同等优先
